{
  "name": "server",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "test": "tsx src/index.ts",
    "build": "esbuild --platform=node --bundle src/index.ts --outfile=index.js"
  },
  "dependencies": {
    "@sctavern/emulator": "*"
  },
  "devDependencies": {
    "esbuild": "^0.17.6",
    "tsx": "^3.12.3"
  }
}
